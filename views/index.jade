doctype html
html
    head
        meta(http-equiv='Content-Type', content='text/html; charset=utf8')
        title Upload Image
        style
            include ../public/style.css
        script
            include ../public/script.js
    body
        h1 Image Uploader
        h4 Please upload an image (recommended size - 1024 x 1024):
        form(action='/form', method='POST', enctype='multipart/form-data', onsubmit='return formSubmit()')
            - each l in layouts
                input(type='hidden', name=(l[0])+'_X' value='0')
                input(type='hidden', name=(l[0])+'_Y' value='0')
            input(type='file', name='img', onchange='fileInspector(this)')
            input(type='submit', name='submit', value='Submit')

        h2 Select the area you wish to crop    
        - each layout in layouts
            h3=layout[0]
            canvas(width='1024', height='1024', id=(layout[0])+'_canvas', data-width=(layout[1]), data-height=(layout[2]))